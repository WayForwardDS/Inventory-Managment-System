services:
  - type: web
    name: your-backend
    env: node
    buildCommand: npm run build
    startCommand: npm start
    envVars:
      - key: MONGO_URI
        value: your-mongodb-atlas-uri
      - key: JWT_SECRET
        value: your-secret-key
